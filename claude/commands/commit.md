---
allowed-tools: Bash(git add:*), Bash(git status:*), Bash(git commit:*), Bash(git diff:*), Bash(git log:*), Bash(git branch:*), Bash(git switch:*)
description: 日本語で詳細なコミットメッセージを生成してコミットする
---

# /commit スキル

**すべての出力は日本語で記述すること。**

## ステップ1: 現状把握

以下のコマンドを**並列**で実行:

- `git status`（未追跡ファイル確認。`-uall`は使わない）
- `git diff --staged`（ステージ済みの変更内容）
- `git diff`（未ステージの変更内容）
- `git log --oneline -5`（直近のコミットメッセージのprefixスタイル参照用）
- `git branch --show-current`（現在のブランチ名）

## ステップ2: ブランチ判定

- **main/masterブランチの場合**: ブランチ作成が必要。ブランチ名を決定する（確認はステップ5でまとめて行う）
- **それ以外のブランチの場合**: そのまま続行

ブランチ名のルール:
- `/` は使わない。`-` で区切る（例: `fix-typo-in-readme`, `feat-add-login-page`）

## ステップ3: ステージング判定

このステップではステージ対象を**判断するだけ**。実際の `git add` 実行はステップ5（ユーザー承認後）で行う。

- 会話の文脈や実装状況から、コミットに含めるべきファイルを判断する
- ステージ済みファイルがある場合でも、未ステージの変更に会話の文脈上コミットすべきファイルがあれば追加対象に含める
- `.env`、認証情報等のファイルが含まれる場合は警告し、ステージ対象から除外する
- `git add .` や `git add -A` は使わない。個別ファイル指定のみ

## ステップ4: コミットメッセージ生成

変更理由が不明な場合はユーザーに質問する。

### 基本原則: 「なぜ」を記載する

変更内容（what）は `git diff` で確認できるが、変更理由（why）はコミットメッセージにしか記録できない。diffから読み取れる情報の優先度は低い。**会話の文脈**から以下を読み取りメッセージの主軸にする:

- **何が問題だったのか**（背景・課題）
- **このコミットでどう改善されるのか**（効果・目的）

typo修正などの自明な変更を除き、**必ず変更が必要だった理由を記載する。**

### 主観的な判断には客観的根拠を添える

「不要」「不適切」「古い」「冗長」などの主観的な判断を含むコミットメッセージは、将来の開発者がその妥当性を検証できない。**なぜそう判断したのか**の根拠を必ず記載する。

❌ 悪い例:
```
不要なコードを削除
```

✅ 良い例:
```
未使用のUserSerializer classを削除

grepで検索した結果、プロジェクト内に呼び出し元が存在しない。
2024年のAPI v2移行時に旧エンドポイントと共に不要になったもの。
```

根拠として有効な情報の例:
- **検索結果**: 「grepで呼び出し元がない」「参照箇所が0件」
- **意思決定の経緯**: 「MTGで使用しないことが決まった」「issueで廃止が合意された」
- **技術的事実**: 「この機能はv2で別の実装に置き換えられた」「このライブラリはEOLを迎えた」
- **エラーや不具合の証拠**: 「本番でこのエラーが発生している」「CIでこのテストが失敗する」
- **数値データ**: 「レスポンスタイムが200ms→50msに改善」「メモリ使用量が16MB削減」

### コミットメッセージの構造

```
1行目：[任意 型(スコープ): ]変更内容の簡潔な要約（50カラム以内推奨）

3行目以降：変更理由の詳細説明（問題の説明 → 解決方法の順）
```

- **1行目**: 50カラム以内を推奨（最大72カラム）、命令形（「〜を追加」「〜を修正」「〜を削除」）、末尾に句読点は不要。カラム数は半角=1、全角=2で計算（日本語中心なら約25文字が目安）
- **2行目**: 空行
- **3行目以降**: 変更理由を詳しく説明（自明な変更を除き必須）。72カラム程度で改行（日本語中心なら約36文字が目安）。具体的な数値やデータ、関連するissue/PR番号があれば含める

### 本文が省略できる場合

変更内容が1行のサマリーで完全に理解でき、「なぜ」が自明な場合のみ省略可:
- typo修正
- lint/フォーマット修正

「不要コード削除」は本文省略不可。なぜ不要と判断したかの根拠が必要。

### Conventional Commits prefix

prefixの利用は**任意**。直近のコミットログでprefixが使われている場合はスタイルを合わせる。使われていなければ付けなくてよい。

ただし、prefixを使う場合は以下のフローチャートで型を選ぶ:

```
ユーザーから見て新しい機能・振る舞いが追加される？
  → Yes → feat
  → No  → ユーザーが困っている不具合を修正した？
            → Yes → fix
            → No  → chore（より具体的な型があればそちらを使ってもよい）
```

判断基準:
- `feat`: リリースノートに「新機能」として書けるか？
- `fix`: ユーザーやシステムが困っている不具合があるか？
- `chore`: リリースノートに書く必要がない裏方の作業か？

その他の型（任意）: `refactor`, `perf`, `style`, `test`, `docs`, `ci`, `build`

破壊的変更がある場合は型の後に `!` を付ける（例: `feat!: APIレスポンスの形式を変更`）

### 過去のコミットログの参照について

過去のコミットログは**prefixスタイルの参照のみ**に使う。過去のコミットメッセージの品質が高い保証はないため、メッセージの内容・詳細度・書き方を過去に合わせてはならない。常にこのスキルのルールに従って高品質なメッセージを生成する。

### コミット粒度

独立してリバートできるアトミックな単位。変更理由ごとにコミットを分ける。ステージ済みの変更が複数の論理的変更を含む場合、コミットの分割を提案する。

## ステップ5: ユーザー確認（1回のみ）

以下の内容をまとめて1回だけ確認する:

- **ブランチ**: 新規作成する場合はブランチ名、既存ブランチの場合はそのブランチ名
- **ステージ対象**: ステージするファイル一覧（既にステージ済みの場合はその旨）
- **コミットメッセージ**: 生成したメッセージ全文

コミット分割を提案する場合は、全コミット分をこの1回の確認にまとめて提示する（コミットごとにステージ対象とメッセージを明記）。

ユーザーの承認後、以下を順次実行:
1. ブランチ作成（必要な場合）: `git switch -c {ブランチ名}`
2. コミットごとに以下を繰り返す:
   a. 対象ファイルをステージ: `git add` で個別にステージ
   b. コミット実行（HEREDOC形式）:

```bash
git commit -m "$(cat <<'EOF'
コミットメッセージ1行目

本文（必要な場合）
EOF
)"
```

## ステップ6: hook失敗時の対応

pre-commit hookが失敗した場合:

1. エラー内容を分析
2. 問題を修正
3. 修正ファイルを再ステージ
4. **新規コミット**を作成（`--amend` は絶対に使わない。hookが失敗した場合コミットは作成されていないため、`--amend`は前のコミットを書き換えてしまう）

## 禁止事項

- Co-Authored-By フッターを追加しない
- コミットメッセージは日本語で記述する（タイプ接頭辞は英語）
- `git push` はこのスキル内では実行しない（ユーザーが明示的に依頼した場合のみ）
- `--amend` は使わない
- `--no-verify` は使わない
- `.env`、認証情報、シークレットファイルのコミットは警告する

